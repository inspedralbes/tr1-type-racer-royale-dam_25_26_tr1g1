# For development, if running frontend outside Docker and backend on localhost, or if backend Docker container exposes port 5000 to host.
# If both frontend and backend are in Docker, 'backend' service name should be used for inter-container communication.
VITE_API_URL=http://localhost:5000
VITE_WS_BASE_URL=ws://localhost:5000